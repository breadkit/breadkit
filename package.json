{
  "name": "breadkit-site",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "mkdir -p _site/assets && cp site/index.html _site/index.html && cp site/favicon.svg _site/favicon.svg && tailwindcss -i site/styles.css -o _site/assets/site.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
